# railway-controller

A monorepo powered by [Bun](https://bun.sh) that consumes Railway's GraphQL API.

## Structure

- `apps/*` — applications
- `packages/*` — shared packages

## Getting started

```bash
bun install
```
